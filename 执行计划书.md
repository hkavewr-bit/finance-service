# 金融客服（金融小二）项目 · 执行计划书

> 版本 v1.0 ｜ 待用户审批。审批通过后进入代码开发与测试阶段。

---

## 一、项目目标

在 `E:\Qoderbasehome\金融小二` 下，基于已有**数据中台 `finance-data`**（业务 API 层）与**参考项目「电商小二」`sz260331`**（架构范式），构建一套**金融客服前后端**，做到：

1. **重新设计业务线**：把「电商订单/商品/退款」的业务线，重构为「银行账户/交易/贷款/银行卡挂失/投诉工单/理财与产品咨询」的金融业务线。
2. **后端**：复刻电商小二 `atguigu` 包的引擎骨架（意图规划 + 知识检索 + 任务流 + 闲聊 + 澄清 + 状态持久化），替换业务 Action / Provider / flow 配置 / prompt 为金融领域。
3. **前端**：Vue3 + Vite 聊天调试页面，支持文本 + 业务对象卡片 + 建议词 + SSE 流式。
4. **打通全链路**：前端 → 客服后端 → 数据中台 → MySQL，端到端可演示、可验收。

---

## 二、现有资产盘点

### 2.1 数据中台 `finance-data`（不动其核心，仅做最小扩展）

- 技术：FastAPI + 原生 async MySQL（`app/database.py`），端口 `8000`，库 `finance`。
- 鉴权：`Authorization: Bearer <customer_no|employee_no>`、`X-Channel-Code`、`X-Operator-No`、`X-Request-Id`。
- 响应信封：`{code:0, message:"ok", request_id, data}`；幂等 `request_no`。
- 关键可用接口（`/api/v1` 前缀）：
  - 客户：`POST /customers`、`GET /customers/{customer_no}`
  - 账户：`GET /customers/{customer_no}/accounts`、`GET /accounts/{account_no}`、`GET /accounts/{account_no}/transactions`、`GET /accounts/{account_no}/ledgers`
  - 银行卡：`POST /accounts/{account_no}/cards`（建卡，返回 `card_no`）
  - 交易：`POST /transactions`、`GET /transactions/{transaction_no}`
  - 贷款：`GET /loan/products`、`GET /loan/products/{code}`、`GET /customers/{customer_no}/credit-limits`、`POST /credit/applications`、`POST /loan/applications`、`GET /loan/applications/{no}`、`GET /loan/contracts/{no}`
  - 理财：`GET /wealth/products`、`GET /wealth/products/{code}`、`GET /customers/{customer_no}/wealth/positions`
  - 工单：`POST /support/tickets`（投诉/工单）、`GET /support/tickets/{ticket_no}`
  - 基础：`GET /branches`、`GET /channels`、`GET /currencies` 等
- 数据生成：`init_db.py`（重建库）+ `generate.main --profile full`（9 层，约 90 张表）+ `generate.insert_support`。

### 2.2 参考项目「电商小二」`sz260331`

- 后端 `customer-service-backend`：Python 包 `atguigu`，FastAPI + LangChain(OpenAI 兼容 LLM) + SQLAlchemy Async + MySQL + YAML 流程引擎 + Jinja2 prompt。
- 分层：`api → services → engines(DialogueEngine) → {plan(TurnPlanner), knowledge(Provider/Responder), task(CommandProcessor/FlowExecutor/ActionRunner), chitchat, clarify}`；`domain`（状态机/消息/上下文）；`Infrastructure`（db/http/llm）；`repository`（状态持久化到 `dialogue_record`）。
- 前端 `customer-service-frontend`：Vue3 + Vite + 数字人 SDK（`lm-avatar-chat-sdk`），带聊天 Turn 卡片、业务对象侧栏、建议词、WS 音频。

> 本计划复刻其**后端架构与状态机**，前端只保留「聊天 + 业务对象卡片 + 建议词」骨架，**移除数字人 SDK 依赖**。

---

## 三、业务线重新设计（核心）

### 3.1 六大能力域

| 能力域 | 金融化落点 |
|---|---|
| 信息检索 | 账户余额 / 银行卡信息 / 交易流水 / 贷款产品 / 理财净值与产品 / FAQ(利率·手续费·政策) |
| 任务型对话 | 账户余额查询、交易流水查询、贷款申请、**银行卡挂失**、**投诉工单** |
| 对话管控 | 槽位收集、上下文保持、流程中断/恢复/取消（复用状态机） |
| 闲聊兜底 | 问候/寒暄/情绪安抚（LLM） |
| 响应模式 | 非流式（JSON）+ SSE 流式（文本逐字） |
| 会话管理 | 创建会话/历史/状态查询/持久化/恢复/清空 |

### 3.2 任务流程清单（`flow_config/user_flows.yml` 待实现）

| flow_id | 名称 | 关键槽位(slots) | 调用的数据中台接口 |
|---|---|---|---|
| `account_balance_query` | 账户余额查询 | `account_no`(或 `card_no`) | `GET /accounts/{account_no}` 或 `GET /customers/{no}/accounts` |
| `transaction_query` | 交易流水查询 | `account_no`、可选 `date_range` | `GET /accounts/{account_no}/transactions` |
| `loan_application` | 贷款申请 | `loan_product`、`apply_amount`、`apply_term_months`、`repayment_method`、`loan_purpose` | `GET /loan/products` → `GET /customers/{no}/credit-limits` → `POST /loan/applications` |
| `card_loss_report` | 银行卡挂失 | `card_no`、`loss_reason`、`identity`(证件号) | `POST /support/tickets`（`ticket_type=card_loss`，`related_type=none`，卡号/原因/证件号写入 `ticket_content`，返回工单号作为挂失回执）|
| `complaint_ticket` | 投诉工单 | `ticket_type`、`ticket_title`、`ticket_content` | `POST /support/tickets`（`related_type=none`，内容写入工单）|

> 每个任务流程：`start` → 逐槽 `collect`（缺失追问）→ `action`（调用中台）→ `action_listen` 结束/回执；支持中断恢复（复用 `paused_tasks`）、取消（复用 `cancel_active_task`）。

### 3.3 知识意图清单（`knowledge/intents.py` 待重写）

| intent | provider | 数据来源 |
|---|---|---|
| `account_info` | `api.account` | `GET /customers/{no}/accounts` |
| `card_info` | `api.card` | `GET /customers/{no}/accounts` → 卡列表 |
| `transaction_info` | `api.transaction` | `GET /accounts/{no}/transactions` |
| `loan_product_info` | `api.loan_product` | `GET /loan/products` |
| `wealth_product_info` | `api.wealth_product` | `GET /wealth/products` |
| `policy_faq` | `faq.default` | 内置静态 FAQ（利率/手续费/挂失流程/销卡规则） |
| `open_knowledge` | `rag.default` | 占位（可后续接向量库） |

### 3.4 前端业务对象（替代订单/商品）

- **账户 Account**（账户号、余额、状态、产品）
- **银行卡 BankCard**（卡号、卡类型/等级、状态）
- **贷款产品 LoanProduct**（产品码、额度区间、期限、年化利率）
- **理财产品 WealthProduct**（产品码、起购金额、预期收益率、风险等级）
- 侧栏 Tab：`账户` / `银行卡` / `贷款产品` / `理财产品`；点击「发送」作为 `object` 消息发给后端，触发信息检索（复用电商的 object → focused_object → set_slots 机制）。

---

## 四、系统架构

### 4.1 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│ 浏览器 (Vue3 + Vite, :5174)                                  │
│  · 聊天 Turn 卡片 / 业务对象卡片 / 建议词 / SSE 流式           │
└───────────────┬─────────────────────────────────────────────┘
                │ /api/* (Vite proxy → 18082)
┌───────────────▼─────────────────────────────────────────────┐
│ 金融客服后端 (FastAPI, :18082)   Python 包 `jinrong`          │
│  api → service → DialogueEngine                              │
│    ├─ TurnPlanner (LLM → TurnPlan JSON)                      │
│    ├─ KnowledgeHandler (Provider→Chunk→LLM 应答)              │
│    ├─ TaskHandler (CommandProcessor + FlowExecutor + Action)  │
│    ├─ ChitchatHandler / ClarifyResponder                     │
│  domain(DialogueState 状态机) / repository(MySQL 持久化)      │
└───────┬──────────────────────────────┬───────────────────────┘
        │ HTTP (Bearer + X-Channel-Code)│  MySQL(库 finance 同实例)
┌───────▼──────────────────────────────▼───────────────────────┐
│ 数据中台 finance-data (FastAPI, :8000)   →  MySQL `finance`   │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 后端目录（复刻 `atguigu`，更名为 `jinrong`）

```
finance-customer-service-backend/
├── pyproject.toml / .env
├── flow_config/{system_flows.yml,user_flows.yml}
├── jinrong/
│   ├── main.py  api/{app,chat_router,schemas,dependencies}.py
│   ├── services/dialogue_service.py
│   ├── engines/{dialogue_engine,builder}.py
│   ├── plan/{turn_plan,planner,validator}.py
│   ├── knowledge/{intents,handler,responder}.py  provider/{provider,knowlege}.py
│   ├── task/{commands,flows,action}/…  action/customer/*.py（金融 Action）
│   ├── chitchat/  clarify/  chat_history/  prompt/{loader,jinja2/*}
│   ├── domain/{state,messages,contexts}.py
│   ├── Infrastructure/{db_client,http_client,llm_client}.py
│   └── repository/{base,dialogue_record,dialogue_repository}.py
```

- **新增/改动相对参考项目**：
  1. `config/settings.py`：`commerce_api_base_url` → `finance_api_base_url`（指向 `http://127.0.0.1:8000`）；新增 `channel_code`、`customer_no`（默认演示身份）。
  2. `Infrastructure/http_client.py`：封装 `finance_*` 请求，统一注入 `Authorization/X-Channel-Code/X-Operator-No/X-Request-Id` 并解包 `{code,message,data}`。
  3. `api/chat_router.py`：新增 `POST /api/session`、`GET /api/session/state`、`DELETE /api/session`、`POST /api/chat/stream`（SSE）。
  4. `domain/messages.py`：`FocusedObject.type` 扩展 `account/card/loan_product/wealth_product/ticket`；`dialogue_engine._try_build_set_slots_command` 的 object→slot 映射改为金融对象。

### 4.3 与数据中台集成规范

- 所有调用走 `finance_api_base_url + /api/v1/...`。
- 请求头：`Authorization: Bearer {customer_no}`、`X-Channel-Code: MOBILE_BANK`、`X-Operator-No: EMP000006`（客服坐席）、`X-Request-Id: {uuid}`。
- 响应处理：`code==0` 取 `data`；`code!=0` 抛业务错误并反馈用户。
- 演示身份：用生成脚本产出的第一个客户 `customer_no`（`CUS…`）作为前端 `sender_id`，保证账户/卡/流水/额度数据存在。

---

## 五、前端设计

```
finance-customer-service-frontend/
├── package.json  vite.config.js  index.html
└── src/{main.js, App.vue}
```

- **技术**：Vue3 `<script setup>` + Vite（仅 `vue` 依赖，**移除** `lm-avatar-chat-sdk`）。
- **布局**（复用电商深色玻璃拟态风格，改金融配色：青绿→金/蓝）：
  - 顶部 header：标题「金融客服系统」+ 客服信息（`金融小二`）+「新对话」。
  - 左侧 controls：`sender_id`（= 客户号，默认演示客户）+「刷新对象列表」。
  - 中间 main：Turn 卡片聊天区（用户文本/对象卡片、客服文本/对象卡片、建议词 chips）、输入框、typing 指示、SSE 流式接收。
  - 右侧 sidebar：Tab「账户/银行卡/贷款产品/理财产品」，卡片带「发送」按钮。
- **接口对接**：
  - `POST /api/chat`（非流式）：body `{sender_id, text|object}`，返回 `{message_id, messages:[{text, object, suggestions}]}`。
  - `POST /api/chat/stream`（SSE）：逐字推 `bot_text`，结尾 `turn_end`。
  - `GET /api/chat/history?sender_id=`、`GET /api/session/state?sender_id=`、`POST/DELETE /api/session`。
- **业务对象卡片字段**：账户（账户号/余额/状态）、银行卡（卡号/卡等级/状态）、贷款产品（产品码/额度区间/年化利率）、理财产品（产品码/起购/收益率/风险等级）。

---

## 六、数据与配置

### 6.1 数据中台准备（一次性）

```bash
cd finance-data
python init_db.py                       # 重建 finance 库
python -m generate.main --profile full  # 9 层生成数据
python -m generate.insert_support       # 补工单样本（若有）
python -m uvicorn app.main:app --port 8000
```

### 6.2 后端 `.env`（关键项）

```
LLM_MODEL=… / LLM_API_KEY=… / LLM_BASE_URL=…      # OpenAI 兼容
DATABASE_URL=mysql+aiomysql://root:123321@127.0.0.1:3306/finance
FINANCE_API_BASE_URL=http://127.0.0.1:8000
CHANNEL_CODE=MOBILE_BANK
OPERATOR_NO=EMP000006
APP_HOST=127.0.0.1 / APP_PORT=18082
```

### 6.3 flow / prompt / 前端演示文案

- `system_flows.yml`：系统流程（任务开始/中断/恢复/取消/澄清/槽位收集提示）沿用并微调措辞。
- `user_flows.yml`：新增 5 条金融任务流程（§3.2）。
- `prompt/jinja2/`：`turn_plan.jinja2`（注入金融 flow 清单 + 金融 intent 清单 + 金融对象类型）、`clarify_respond`、`knowledge_respond`、`chitchat_respond` 金融化改写。

---

## 七、分阶段实施步骤（里程碑）

> 每阶段结束均有可运行的验证点。

| 阶段 | 内容 | 验证点 |
|---|---|---|
| **M0 数据中台就绪** | 初始化库 + 生成数据 + 起服务；确认鉴权/信封/关键接口可用 | `curl` 取到客户/账户/产品数据 |
| **M1 后端骨架** | 复刻 `jinrong` 包：config/Infrastructure/repository/domain；`.env`；`dialogue_record` 建表 | 后端启动、`/health` 通、DB 连接成功 |
| **M2 引擎 + 意图规划** | `TurnPlanner/Validator`、`DialogueEngine`、chat API 非流式打通 | 文本对话返回 LLM 应答 |
| **M3 知识检索** | `knowledge/intents + provider`（账户/卡/交易/贷款/理财/FAQ） | 「查下我的账户」命中 `api.account` 返回真实余额 |
| **M4 任务流 + Action** | `flow_config/user_flows.yml` 5 条流程 + 金融 Action（查询/申请/挂失/工单）+ 自动注册 | 逐流程走通：余额/流水/贷款/挂失/工单 |
| **M5 会话与流式** | `session` 接口 + `SSE` 流式 + 状态持久化/恢复 | 刷新后历史可恢复；流式逐字出 |
| **M6 前端** | Vue3 页面（聊天 + 对象侧栏 + SSE + 建议词） | 浏览器完成 5 条流程闭环演示 |
| **M7 测试与文档** | 自动化测试（pytest）+ README + 演示脚本 | 验收清单全绿 |

---

## 八、测试与验收

**自动化**（`tests/`，pytest + httpx AsyncClient，复刻参考项目风格）：
- 单元：`DialogueState` 状态机 to_dict/from_dict、`FlowExecutor` 槽位收集、Action 注册、`{code,message,data}` 解包。
- 集成：以「演示客户号」为 `sender_id`，跑通 5 条任务流程 + 3 类知识检索 + 闲聊 + 澄清；断言回复含真实中台数据。

**验收清单（对齐需求书）**：
1. ✅ 账户余额查询、交易流水查询 返回真实数据。
2. ✅ 贷款申请全流程（选产品→额度校验→提交→回执单号）。
3. ✅ 银行卡挂失流程（卡号→原因→身份→落一张挂失工单，返回工单号）。
4. ✅ 投诉工单流程（类型→标题→内容→工单号）。
5. ✅ 闲聊兜底、意图澄清、流程中断/恢复/取消。
6. ✅ 非流式 + SSE 流式两种响应。
7. ✅ 会话历史/状态持久化与恢复。
8. ✅ 前端完成一次完整闭环演示。

---

## 九、风险与决策点

1. **【已定案】银行卡挂失 = 用工单兜底**：不改动数据中台，客服后端把「挂失」落为一张 `support_tickets` 工单（`ticket_type=card_loss`）。经实测，数据中台工单 `related_type` 仅支持 `account_transaction/wealth_order/loan_contract/loan_application/repayment_bill/collection_case/support_ticket/risk_event/fee_reduction`，**不支持 `bank_card`**，故挂失工单采用 `related_type=none`，卡号/挂失原因/证件号写入 `ticket_content`。返回工单号作为挂失回执。零改动数据中台，链路真实闭环（工单真实入库、可查询）。
2. **【已定案】LLM 依赖**：用户已有 OpenAI 兼容端点，稍后填入后端 `.env`（`LLM_BASE_URL/API_KEY/MODEL`），意图规划走真实 LLM。
3. **贷款申请审批链路长**：`POST /loan/applications` 提交后审批/签约/放款需多步，客服流程以「提交申请 + 返回申请单号」为验收终点（不演示放款）。
4. **`eval()` 条件**：参考项目 flow 条件用 `eval()`，复刻时保留但限制为白名单变量，避免安全风险。
5. **端口约定**：数据中台 `8000`、客服后端 `18082`、前端 `5174`；如需改端口，`.env` 与 `vite.config.js` 同步调整。

---

> 请审批本计划书。**重点确认 §9 决策点 1（挂失接口：扩展数据中台 vs 用工单兜底）**，以及是否接受「贷款以提交申请为终点、移除前端数字人 SDK」这两处范围界定。确认后我将从 M0 开始开发与测试。
